//'use strict';

/**
 * @ngdoc function
 * @name tpanalyticsApp.controller:ScorecardBizStatsController
 * @description
 * # ScorecardBizStatsController
 * Controller of the tpanalyticsApp
 */
angular.module('tpanalyticsApp')
    .controller('ScorecardBizStatsController',['$scope', '$rootScope', '$http', 'queryService', function($scope, $rootScope, $http, queryService) {
		// Test for proper page context: alert('in category controller rootscope.stateparams is set to:' + $rootScope.$stateParams.pagecontext);
        //queryService.calculateBizMetrics();
	
	/* Code to add remove selected class for sidebar category table in scorecard view */
	$scope.selected = 0;

    $scope.select= function(index) {
       $scope.selected = index; 
    };
	/* End code to add remove selected class for sidebar category table in scorecard view */
}]);